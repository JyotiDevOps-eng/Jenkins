pipeline {
  agent none
  stages {
    stage ("Backend") {
      agent {
        docker { image 'maven:3.3.8-adoptopenjdk:12.0.1' }
      }
      steps {
        sh 'mvn --version'
      }
    }
  }
}
